# PodBridge requires NO secrets or environment variables to build or run.
# It is a local, offline tool (reads local Bluetooth only; no API keys, no cloud).
# This file exists only to document that fact for the loop-readiness checklist.
